// Create three variables, each using unique operation
// Create a message for the user
// Create the alert with message and combination


//Create the Display Intro Message:

const introMessage = "You have received this message because you have been chosen to open an important vault. Here is the secret combination:"

//Calculate the sum of numbers to get combo using arithmetic operators:
//Operators below
const number1 = 0 + 10;
const number2 = 20 * 2;
const number3 = 42 - 3;

// Create message with combo and original text

const combinationMessage = `${introMessage}\n${number1} - ${number2} - ${number3}`;

//Create an alert box with combination in dialog box
alert(combinationMessage);